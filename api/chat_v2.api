syntax = "v1"

@server (
    group : v2/chat
    prefix : /v2/chat
    jwt : Auth
)

service imy-api {
    @doc (
        summary:"私聊"
    )
    @handler SingleChat
    post /single (SingleChatReq) returns (SingleChatResp)
}

type SingleChatReq {
    RevId uint32 `json:"revId"`

}

type SingleChatResp {

}